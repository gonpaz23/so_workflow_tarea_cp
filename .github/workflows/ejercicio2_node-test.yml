name: Ejercicio 2 

on: [push]

jobs:
  test-node:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18.x'
    
    - name: Configurar proyecto básico
      run: |
        # Crear package.json si no existe
        if [ ! -f "package.json" ]; then
          npm init -y
        fi
        
        # Crear archivo JavaScript
        echo "console.log('✅ Node.js funcionando en GitHub Actions');" > app.js
        
        # Crear test simple
        echo "console.log('🧪 Test ejecutado exitosamente');" > test.js
    
    - name: Ejecutar aplicación
      run: node app.js
    
    - name: Ejecutar tests
      run: node test.js